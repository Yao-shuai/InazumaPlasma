/**
 * @file        main.c
 * @brief       InazumaKV 存储引擎主程序入口
 * @author      Nexus_Yao
 * @date        2026-XX-XX
 * @version     1.0.0
 * @note        该文件作为整个系统的引导入口 (Bootstrap Entry)，
 * 仅负责进程生命周期的接管，并将命令行参数透传至核心引擎守护进程。
 * 保持入口文件的绝对轻量化，符合模块解耦与隔离的架构规范。
 */

#include <stdio.h>

/* ==========================================================================
 * 核心引擎引导钩子声明 (Core Engine Bootstrap Hook)
 * ========================================================================== */

/** * @brief 声明外部的核心引擎主事件循环启动函数 
 * @note  该函数定义于核心网络层模块中，负责接管后续所有的系统初始化与请求调度。
 */
extern int kvs_server_start(int argc, char *argv[]);


/* ==========================================================================
 * 进程主入口 (Main Entry Point)
 * ========================================================================== */

/**
 * @brief       标准 C 语言进程入口函数
 * @param[in]   argc  命令行参数的数量
 * @param[in]   argv  命令行参数的字符串数组指针
 * @return      int   0: 引擎正常退出; 非0: 引擎异常终止返回码
 */
int main(int argc, char *argv[]) {
    /* * 纯粹的入口透传调用，不在此处进行任何资源分配，
     * 确保引擎的初始化操作具备完整的上下文连贯性。
     */
    return kvs_server_start(argc, argv);
}